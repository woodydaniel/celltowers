# CellMapper Tower Scraper Dependencies
# Python 3.10+ required

# HTTP client with async support
httpx>=0.25.0

# TLS-client: Chrome-grade TLS fingerprint (bypasses fingerprint detection)
tls-client>=1.0.0

# Redis client for cookie pool
redis>=5.0.0

# Async HTTP client (for CAPTCHA providers)
aiohttp>=3.9.0

# Data processing (optional, for post-processing)
pandas>=2.0.0

# Geographic utilities (optional)
geopy>=2.4.0

# Progress bars (optional)
tqdm>=4.66.0

# Auto cookie refresh (for headless servers)
# After pip install, also run: playwright install chromium
playwright>=1.40.0
playwright-stealth>=2.0.0

# Email notifications (Resend)
resend>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

